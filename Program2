Aim: To implement Singly Linked List and Stack 
 Program: Write a program to implement a Singly Linked List and perform Insert at the front and end,Delete at the front and traverse the list.
	Test Linked List:  
•	Insert [10,20,30] 
•	Delete front 
 Program: Write a program to implement a Stack and perform push, pop, peek, isEmpty and traverse the Stack.
	Test Stack: 
•	Push [5,10] 
•	Pop once 
•	Peek 
Description: 
1)This program implements a Singly Linked List data structure using a Node class containing data and next pointer. The LinkedList class provides methods to:
•	Insert nodes at the front (beginning) of the list
•	Insert nodes at the end of the list
•	Delete the front node
•	Traverse and display all nodes in the list
The program is tested with inserting values [10, 20, 30], deleting the front node, and displaying the resulting list.
2) This program implements a Stack using Linked List where the top of the stack is maintained at the front of the list. The Stack class provides essential operations:
•	Push: Insert an element at the top of the stack
•	Pop: Remove and return the top element
•	Peek: View the top element without removing it
•	isEmpty: Check if the stack is empty
The program is tested by pushing values [5, 10], performing one pop operation, and peeking at the top element.
Code:
class node:
    def __init__(self,data):
        self.data=data
        self.next=None

class sll:
    def __init__(self):
        self.head=None

    def insertion(self,data):
        new=node(data)
        new.next=self.head
        self.head=new

    def insert_end(self, data):
        new = node(data)
        if self.head is None:
            self.head = new
        else:
            temp = self.head
            while temp.next:
                temp = temp.next
            temp.next = new

    def delete(self):
        temp=self.head
        self.head=temp.next
        temp=None

    def display(self):
        if self.head is None:
            print("list is empty")
        else:
            temp=self.head
            while temp:
                print(temp.data,"-->",end=" ")
                temp=temp.next

class stack:
    def __init__(self):
        self.top=None

    def push(self,data):
        new=node(data)
        new.next=self.top
        self.top=new

    def pop(self):
        if self.top is None:
            print("stack is empty")
        else:
            print("poped element is: ",self.top.data)
            self.top=self.top.next

    def peek(self):
        if self.top is None:
            print("stack is empty")
        else:
            print("top element is ",self.top.data)

    def display(self):
        if self.top is None:
            print("stack is empty")
        else:
            temp=self.top
            while temp:
                print(temp.data,"-->",end=" ")
                temp=temp.next

            



pp=sll()

n=int(input("how many values you want to inset: "))
for i in range(n):
    val=int(input("enter the value: "))
    pp.insert_end(val)

pp.display()
print("the list\n")
pp.delete()

pp.display()


sc=stack()

n=int(input("\nenter how many element to be inserted: "))
for i in range(n):
    val=int(input("enter the elements: "))
    sc.push(val)

sc.display()

sc.pop()
sc.display()

